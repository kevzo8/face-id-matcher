version: "3.9"

services:
  liveness-backend:
    build: .
    container_name: svi-liveness
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: add an Nginx reverse proxy in front for HTTPS later.
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 3
